/**
 * @description
 * vertex as a alias of a number, which represent the value of a vertex. Vertex as a component of a 'Graph',
 * it can be present in a 'Edge' as well.
 * 
 * @example
 * ```ts
 * // create a vertex with value = 1
 * const vertex: Vertex = 1;
 * ```
 */
export type Vertex = number;